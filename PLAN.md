# PLAN.md

## 还原计划

- 当前保留的方向：
  - `混合`
  - `循环`
  - `复制 & 旋转`
- 实现方式：继续保持分模块结构，主线程、算法、消息协议和 UI 分开维护。

## 当前进度

- 已完成 MasterGo 插件的基础分层：
  - `lib/main.ts`
  - `lib/blend.ts`
  - `lib/svgPath.ts`
  - `lib/nodeGroup.ts`
  - `lib/loopGenerator.ts`
  - `lib/copyRotateGenerator.ts`
  - `messages/sender.ts`
  - `ui/App.vue`
- 已完成一个可运行的混合工具 MVP：
  - 选中两个同父级图层
  - 输入中间层数
  - 生成插值副本
- 已完成步骤渐变外观：
  - 每一步都会按位置线性渐变颜色和透明度
- 已恢复路径混合优先：
  - 可提取轮廓的形状优先走路径 morph
  - 不可提取时再回退到基础混合
- 已完成插件样式重构：
  - UI 改成浅色卡片布局
  - 底部有固定按钮
  - 文案统一为中文
- 已完成 UI 模块拆分：
  - `ui/App.vue` 只负责页面装配
  - 交互状态放到独立 composable
  - 表单、状态、底部按钮拆到独立组件
- 已清理无用的形状生成器残留：
  - 删除了旧的辅助组件、空页和相关 composable
- 已完成 tab 入口：
  - `循环`
  - `旋转复制`
  - `混合`
- 已修正 tab 等分布局：
  - 当前只保留 3 个页签，tab 栏按 3 列等分
- 已调整默认打开页：
  - 打开插件默认进入 `循环`
- 已完成 `循环` tab：
  - 采用中文字段
  - 支持迭代次数、位置、缩放、透明度、填充、描边参数
  - 主线程对单图层进行循环复制并生成结果组
- 已给 `循环` tab 增加生成层级控制：
  - 可切换生成结果位于源图层上方或下方
- 已移除 `循环` 页底部状态展示：
  - 页面不再显示单独的状态文案
- 已清理 `循环` 页的状态死逻辑：
  - 去掉了只用于展示状态的预览分支
- 已移除 `旋转复制` 页状态层：
  - 页面不再显示 `.status` 文案
- 已完成 `复制 & 旋转` tab：
  - 支持总数量、半径、旋转步长、起始角度、控制点
  - 支持画布预览和径向对齐副本开关
  - 生成结果会按组件逻辑落到画布中
- 已增强 `混合` 的外观回退：
  - 对不完全匹配的填充 / 描边，优先保留可用外观而不是直接清空
  - 对可插值的描边权重和圆角，尽量保留中间态
  - 路径混合、引导混合、循环复用同一套外观处理逻辑
- 已调整 `循环` 的描边布局：
  - 4 个描边相关字段合并到同一个 4 列容器里，一行展示
- 已增强 `循环` 的颜色输入：
  - 颜色字段收敛成单一浏览器原生颜色控件
  - 默认不再展示占位 Hex 值，空值保持空白
  - 空值时颜色点回退为白色圆点
  - 颜色点改成圆形按钮样式
  - 颜色点尺寸收窄到 12px
- 已修正 `循环` 的单边颜色生效：
  - 只选开始色或只选结束色时也会应用到生成层
  - 两边都填写时才执行渐变
- 已统一 `循环` 的填充 / 描边单边逻辑：
  - 填充和描边共用同一套颜色应用规则
- 已给 `循环` 底部增加恢复默认按钮：
  - 一键回到默认参数
- 已收窄 `循环` 底部恢复默认按钮：
  - 去掉取消按钮，恢复默认按钮更紧凑

## 下一步

- 继续增强 `混合` 质量：
  - 更稳健的图层类型兼容
  - 更稳定的命名和层级保留
- 继续打磨 `循环`：
  - 让控制点、数值拖拽和样式更贴近参考
- 继续打磨 `复制 & 旋转`：
  - 保持预览稳定
  - 收紧层级结构和命名
- 如果后续要补新功能，优先挂到已有三个 tab 里，继续保持页面级拆分。
